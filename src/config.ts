
const l = 42, // 滑块边长
  r = 7.5

const config = {
  l, // 滑块边长
  r, // 滑块半径
  svCanvasWidth: 310, // canvas宽度
  svCanvasHeight: 210, // canvas高度
  L: l + r * 2 + 9, // 滑块实际边长
}

export default config